apiVersion: 1

providers:
  - name: 'Vector Dashboard'
    orgId: 1
    folder: 'Vector'
    folderUid: 'vector'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
